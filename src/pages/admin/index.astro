---
import Layout from "../../layouts/Layout.astro";
import { supabaseAdmin } from "../../lib/supabaseAdmin";
import { requireAdmin } from "../../lib/adminAuth";
import { fetchUsersMap } from "../../lib/userLookup";
import { COST_PER_CARTON } from "../../lib/orderPricing";

export const prerender = false;

const admin = await requireAdmin(Astro);
if (!admin) {
	return Astro.redirect("/admin/login");
}

if (!supabaseAdmin) {
	throw new Error("Missing admin client.");
}

const { data: ordersData } = await supabaseAdmin
	.from("orders")
	.select(
		"id, order_number, user_id, preorder_id, cartons, total, payment_method, status, created_at, payment_proof_path",
	)
	.order("created_at", { ascending: false });

const ordersRaw = ordersData ?? [];
const paymentProofUrls = new Map<string, string>();

await Promise.all(
	ordersRaw.map(async (order) => {
		const proofPath = String((order as any).payment_proof_path || "");
		if (!proofPath) return;
		const { data } = await supabaseAdmin.storage
			.from("payment-proofs")
			.createSignedUrl(proofPath, 60 * 60);
		if (data?.signedUrl) paymentProofUrls.set(order.id, data.signedUrl);
	})
);

const { data: orderFlavorsData } =
	ordersRaw.length > 0
		? await supabaseAdmin
				.from("order_flavors")
				.select("order_id, flavor_id, quantity")
				.in(
					"order_id",
					ordersRaw.map((order) => order.id),
				)
		: { data: [] };

const { data: flavorsData } = await supabaseAdmin
	.from("flavors")
	.select("id, name");
const { data: preordersData } = await supabaseAdmin
	.from("preorders")
	.select("id, name, start_date, end_date, status")
	.order("end_date", { ascending: false });
const { data: promoCodesData } = await supabaseAdmin
	.from("promo_codes")
	.select("id, code, type, value, min_cartons, max_uses, is_active, starts_at, ends_at, created_at")
	.order("created_at", { ascending: false });

const users = Array.from((await fetchUsersMap(supabaseAdmin)).values());
const promoCodes = promoCodesData ?? [];

const toNumber = (value: unknown) => {
	const parsed = Number(value ?? 0);
	return Number.isFinite(parsed) ? parsed : 0;
};

const formatDate = (value: string | null | undefined) => {
	if (!value) return "--";
	const date = new Date(value);
	const day = String(date.getDate()).padStart(2, "0");
	const month = String(date.getMonth() + 1).padStart(2, "0");
	const year = date.getFullYear();
	return `${day}/${month}/${year}`;
};

const userMap = new Map(
	users.map((user) => [
		user.id,
		{
			email: user.email || "",
			firstName: user.firstName || "",
			lastName: user.lastName || "",
		},
	]),
);

const orders = ordersRaw.map((order) => ({
	...order,
	cartons: toNumber(order.cartons),
	total: toNumber(order.total),
	createdAt: new Date(order.created_at),
}));

const orderDateById = new Map(
	orders.map((order) => [order.id, order.createdAt]),
);

const orderFlavors = orderFlavorsData ?? [];
const flavorMap = new Map(
	(flavorsData ?? []).map((flavor) => [flavor.id, flavor.name]),
);

const flavorsByOrder = new Map<
	string,
	{ id: string; name: string; quantity: number }[]
>();
const flavorCountByOrder = new Map<string, number>();

for (const item of orderFlavors) {
	const list = flavorsByOrder.get(item.order_id) ?? [];
	const name = flavorMap.get(item.flavor_id) ?? "Inconnu";
	const quantity = toNumber(item.quantity);
	list.push({ id: item.flavor_id, name, quantity });
	flavorsByOrder.set(item.order_id, list);
	flavorCountByOrder.set(
		item.order_id,
		(flavorCountByOrder.get(item.order_id) ?? 0) + quantity,
	);
}

const now = new Date();
const monthStart = new Date(now.getFullYear(), now.getMonth(), 1);
const weekStart = new Date(now.getTime() - 6 * 24 * 60 * 60 * 1000);
weekStart.setHours(0, 0, 0, 0);
const prevWeekStart = new Date(weekStart.getTime() - 7 * 24 * 60 * 60 * 1000);

const ordersThisMonth = orders.filter((order) => order.createdAt >= monthStart);
const ordersThisWeek = orders.filter((order) => order.createdAt >= weekStart);

const revenueMonth = ordersThisMonth.reduce(
	(sum, order) => sum + order.total,
	0,
);
const revenueWeek = ordersThisWeek.reduce((sum, order) => sum + order.total, 0);
const cartonsMonth = ordersThisMonth.reduce(
	(sum, order) => sum + order.cartons,
	0,
);
const marginMonth = ordersThisMonth.reduce(
	(sum, order) => sum + (order.total - order.cartons * COST_PER_CARTON),
	0,
);
const marginPercent = revenueMonth > 0 ? (marginMonth / revenueMonth) * 100 : 0;

const activeClients = new Set(ordersThisMonth.map((order) => order.user_id))
	.size;
const totalRegisteredUsers = users.length;
const uniqueBuyingUsers = new Set(orders.map((order) => order.user_id));
const recurringClients = Array.from(
	orders.reduce((map, order) => {
		map.set(order.user_id, (map.get(order.user_id) ?? 0) + 1);
		return map;
	}, new Map<string, number>()),
).filter(([, count]) => count >= 2).length;
const averageBasketMonth =
	ordersThisMonth.length > 0 ? revenueMonth / ordersThisMonth.length : 0;
const conversionRateMonth =
	totalRegisteredUsers > 0 ? (activeClients / totalRegisteredUsers) * 100 : 0;

const monthlyStats = Array.from({ length: 6 }, (_, index) => {
	const date = new Date(now.getFullYear(), now.getMonth() - (5 - index), 1);
	const nextDate = new Date(date.getFullYear(), date.getMonth() + 1, 1);
	const monthOrders = orders.filter(
		(order) => order.createdAt >= date && order.createdAt < nextDate,
	);
	const revenue = monthOrders.reduce((sum, order) => sum + order.total, 0);
	const clients = new Set(monthOrders.map((order) => order.user_id)).size;
	return {
		label: date.toLocaleDateString("fr-FR", { month: "short" }),
		revenue,
		clients,
	};
});

const maxMonthlyRevenue = Math.max(
	1,
	...monthlyStats.map((stat) => stat.revenue),
);

const dayNames = ["Dim", "Lun", "Mar", "Mer", "Jeu", "Ven", "Sam"];
const dailyStats = Array.from({ length: 7 }, (_, index) => {
	const date = new Date(now.getTime() - (6 - index) * 24 * 60 * 60 * 1000);
	const key = date.toISOString().slice(0, 10);
	const total = orders.reduce((sum, order) => {
		const orderKey = order.createdAt.toISOString().slice(0, 10);
		return orderKey === key ? sum + order.total : sum;
	}, 0);
	return {
		label: dayNames[date.getDay()],
		total,
	};
});

const maxDailyTotal = Math.max(1, ...dailyStats.map((stat) => stat.total));

const flavorTotalsMap = new Map<string, number>();
for (const item of orderFlavors) {
	flavorTotalsMap.set(
		item.flavor_id,
		(flavorTotalsMap.get(item.flavor_id) ?? 0) + toNumber(item.quantity),
	);
}

const flavorStats = (flavorsData ?? [])
	.map((flavor) => ({
		id: flavor.id,
		name: flavor.name,
		total: flavorTotalsMap.get(flavor.id) ?? 0,
	}))
	.filter((flavor) => flavor.total > 0)
	.sort((a, b) => b.total - a.total);

const flavorColors = ["#4f6fff", "#7a4bff", "#b94bff", "#ff7ac6", "#ffb84b"];
const totalFlavorCount = flavorStats.reduce((sum, item) => sum + item.total, 0);
let donutAcc = 0;
const donutSegments = flavorStats.slice(0, 5).map((item, index) => {
	const percent =
		totalFlavorCount > 0 ? (item.total / totalFlavorCount) * 100 : 0;
	const start = donutAcc;
	const end = donutAcc + percent;
	donutAcc = end;
	return {
		...item,
		percent,
		color: flavorColors[index] || "#94a3b8",
		gradient: `${flavorColors[index] || "#94a3b8"} ${start.toFixed(2)}% ${end.toFixed(2)}%`,
	};
});

const donutStyle =
	donutSegments.length > 0
		? `conic-gradient(${donutSegments.map((segment) => segment.gradient).join(", ")})`
		: "conic-gradient(#e2e8f0 0 100%)";

const weekFlavorTotals = new Map<string, number>();
const prevWeekFlavorTotals = new Map<string, number>();

for (const item of orderFlavors) {
	const date = orderDateById.get(item.order_id);
	if (!date) continue;
	const quantity = toNumber(item.quantity);
	if (date >= weekStart) {
		weekFlavorTotals.set(
			item.flavor_id,
			(weekFlavorTotals.get(item.flavor_id) ?? 0) + quantity,
		);
	} else if (date >= prevWeekStart && date < weekStart) {
		prevWeekFlavorTotals.set(
			item.flavor_id,
			(prevWeekFlavorTotals.get(item.flavor_id) ?? 0) + quantity,
		);
	}
}

const openPreorder =
	(preordersData ?? []).find((preorder) => preorder.status === "open") ??
	null;
const defaultOrdersPreorderId = openPreorder?.id ?? "all";
const openOrderIds = new Set(
	orders
		.filter((order) => order.preorder_id === openPreorder?.id)
		.map((order) => order.id),
);
const openOrders = orders.filter((order) => openOrderIds.has(order.id));
const openClients = new Set(openOrders.map((order) => order.user_id)).size;
const openCartons = openOrders.reduce((sum, order) => sum + order.cartons, 0);
const openEndDate = openPreorder?.end_date
	? new Date(openPreorder.end_date)
	: null;
const openRemainingMs = openEndDate ? openEndDate.getTime() - now.getTime() : 0;
const openRemainingLabel =
	openRemainingMs > 0
		? `${Math.floor(openRemainingMs / 3600000)
				.toString()
				.padStart(2, "0")}h ${Math.floor(
				(openRemainingMs % 3600000) / 60000,
			)
				.toString()
				.padStart(2, "0")}m`
		: "Terminee";

const openFlavorTotals = new Map<
	string,
	{ quantity: number; orders: Set<string> }
>();
for (const item of orderFlavors) {
	if (!openOrderIds.has(item.order_id)) continue;
	const entry = openFlavorTotals.get(item.flavor_id) ?? {
		quantity: 0,
		orders: new Set(),
	};
	entry.quantity += toNumber(item.quantity);
	entry.orders.add(item.order_id);
	openFlavorTotals.set(item.flavor_id, entry);
}

const openFlavorStats = (flavorsData ?? [])
	.map((flavor) => {
		const entry = openFlavorTotals.get(flavor.id);
		return {
			id: flavor.id,
			name: flavor.name,
			quantity: entry?.quantity ?? 0,
			orders: entry?.orders.size ?? 0,
		};
	})
	.filter((item) => item.quantity > 0)
	.sort((a, b) => b.quantity - a.quantity);

const formatNumber = (value: number) => Math.round(value).toString();
---

<Layout>
	<main class="admin-shell min-h-screen px-4 py-8 sm:px-6 lg:px-8">
		<div class="mx-auto flex max-w-7xl flex-col gap-6">
			<header class="admin-hero">
				<div>
					<p class="admin-eyebrow">Console admin</p>
					<h1 class="admin-title">Centre de pilotage</h1>
					<p class="admin-subtitle">
						Suivi des commandes, précommandes, marges, codes promo et exports.
					</p>
				</div>
				<div class="admin-hero-card">
					<p class="admin-hero-label">Session</p>
					<p class="admin-hero-value">{admin.email}</p>
					<a href="/admin/logout" class="admin-logout">Se déconnecter</a>
				</div>
			</header>

			<nav class="admin-tabs">
				<button
					class="tab-button tab-active"
					data-tab-target="dashboard"
					type="button"
				>
					Tableau de bord
				</button>
				<button
					class="tab-button"
					data-tab-target="orders"
					type="button">Commandes</button
				>
				<button
					class="tab-button"
					data-tab-target="promos"
					type="button">Codes promo</button
				>
				<button
					class="tab-button"
					data-tab-target="preorders"
					type="button">Précommandes</button
				>
				<a
					href="/admin/logout"
					class="admin-tabs-link"
				>
					Sortie
				</a>
			</nav>

			<section
				data-tab-panel="dashboard"
				class="relative z-10 flex flex-col gap-8"
			>
				<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
					<div class="stat-card">
						<div class="flex items-center justify-between">
							<p class="stat-label text-emerald-600">
								Revenus du mois
							</p>
							<span
								class="flex h-8 w-8 items-center justify-center rounded-full bg-emerald-100 text-emerald-600"
							>
								<svg
									class="h-4 w-4"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path d="M4 10h12"></path>
									<path d="M4 14h9"></path>
									<path d="M19 6a8 8 0 1 0 0 12"></path>
								</svg>
							</span>
						</div>
						<p class="stat-value">
							{formatNumber(revenueMonth)} EUR
						</p>
						<p class="stat-sub">
							+{formatNumber(revenueWeek)} EUR cette semaine
						</p>
					</div>
					<div class="stat-card">
						<div class="flex items-center justify-between">
							<p class="stat-label text-blue-600">Marge totale</p>
							<span
								class="flex h-8 w-8 items-center justify-center rounded-full bg-blue-100 text-blue-600"
							>
								<svg
									class="h-4 w-4"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path d="M3 17l6-6 4 4 8-8"></path>
									<path d="M14 7h7v7"></path>
								</svg>
							</span>
						</div>
						<p class="stat-value">
							{formatNumber(marginMonth)} EUR
						</p>
						<p class="stat-sub">
							{marginPercent.toFixed(1)}% de marge
						</p>
					</div>
					<div class="stat-card">
						<div class="flex items-center justify-between">
							<p class="stat-label text-violet-600">
								Cartons vendus
							</p>
							<span
								class="flex h-8 w-8 items-center justify-center rounded-full bg-violet-100 text-violet-600"
							>
								<svg
									class="h-4 w-4"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path d="M21 8v8l-9 5-9-5V8l9-5 9 5z"
									></path>
									<path d="M12 3v18"></path>
									<path d="M3 8l9 5 9-5"></path>
								</svg>
							</span>
						</div>
						<p class="stat-value">{formatNumber(cartonsMonth)}</p>
						<p class="stat-sub">Ce mois-ci</p>
					</div>
					<div class="stat-card">
						<div class="flex items-center justify-between">
							<p class="stat-label text-amber-600">
								Clients actifs
							</p>
							<span
								class="flex h-8 w-8 items-center justify-center rounded-full bg-amber-100 text-amber-600"
							>
								<svg
									class="h-4 w-4"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path d="M17 21v-2a4 4 0 0 0-3-3.87"></path>
									<path d="M7 21v-2a4 4 0 0 1 3-3.87"></path>
									<circle cx="9" cy="7" r="4"></circle>
									<circle cx="17" cy="7" r="4"></circle>
								</svg>
							</span>
						</div>
						<p class="stat-value">{formatNumber(activeClients)}</p>
						<p class="stat-sub">
							{formatNumber(totalRegisteredUsers)} comptes inscrits
						</p>
					</div>
				</div>

				<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">
					<div class="stat-card">
						<p class="stat-label text-slate-600">Panier moyen</p>
						<p class="stat-value">
							{formatNumber(averageBasketMonth)} EUR
						</p>
						<p class="stat-sub">Par commande ce mois-ci</p>
					</div>
					<div class="stat-card">
						<p class="stat-label text-slate-600">Conversion</p>
						<p class="stat-value">
							{conversionRateMonth.toFixed(1)}%
						</p>
						<p class="stat-sub">
							Clients du mois / comptes inscrits
						</p>
					</div>
					<div class="stat-card">
						<p class="stat-label text-slate-600">
							Clients recurrents
						</p>
						<p class="stat-value">{formatNumber(recurringClients)}</p>
						<p class="stat-sub">2 commandes ou plus</p>
					</div>
					<div class="stat-card">
						<p class="stat-label text-slate-600">
							Commandes du mois
						</p>
						<p class="stat-value">
							{formatNumber(ordersThisMonth.length)}
						</p>
						<p class="stat-sub">
							{formatNumber(uniqueBuyingUsers.size)} clients au total
						</p>
					</div>
				</div>

				<div class="grid gap-6 lg:grid-cols-2">
					<div
						class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
					>
						<div class="flex items-center justify-between">
							<div>
								<p class="text-sm font-semibold text-slate-800">
									Ventes de la semaine
								</p>
								<p class="text-xs text-slate-500">
									Chiffre d'affaires journalier
								</p>
							</div>
							<span
								class="rounded-full bg-[#eef2ff] px-3 py-1 text-xs font-semibold text-[#5a5dff]"
							>
								7 jours
							</span>
						</div>
						<div class="mt-6 flex h-40 items-end gap-2">
							{
								dailyStats.map((stat) => (
									<div class="flex flex-1 flex-col items-center gap-2">
										<div
											class="w-full rounded-full bg-gradient-to-t from-[#4f6fff] via-[#7a4bff] to-[#d24bff] shadow-md shadow-indigo-400/30"
											style={`height: ${Math.max(8, (stat.total / maxDailyTotal) * 100)}%`}
										/>
										<span class="text-xs text-slate-500">
											{stat.label}
										</span>
									</div>
								))
							}
						</div>
					</div>

					<div
						class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
					>
						<div class="flex items-center justify-between">
							<div>
								<p class="text-sm font-semibold text-slate-800">
									Repartition des gouts
								</p>
								<p class="text-xs text-slate-500">
									Top 5 des demandes
								</p>
							</div>
							<span
								class="rounded-full bg-[#f4edff] px-3 py-1 text-xs font-semibold text-[#9b5cff]"
							>
								Donut
							</span>
						</div>
						<div class="mt-6 flex items-center gap-6">
							<div
								class="relative flex h-36 w-36 items-center justify-center"
							>
								<div
									class="h-full w-full rounded-full"
									style={`background: ${donutStyle};`}
								>
								</div>
								<div
									class="absolute inset-5 rounded-full bg-white"
								>
								</div>
								<p
									class="absolute text-xs font-semibold text-slate-500"
								>
									{totalFlavorCount} votes
								</p>
							</div>
							<div class="flex flex-1 flex-col gap-2">
								{
									donutSegments.map((segment) => (
										<div class="flex items-center gap-3 text-xs text-slate-600">
											<span
												class="h-2.5 w-2.5 rounded-full"
												style={`background: ${segment.color};`}
											/>
											<span class="flex-1">
												{segment.name}
											</span>
											<span class="text-slate-500">
												{segment.percent.toFixed(0)}%
											</span>
										</div>
									))
								}
								{
									donutSegments.length === 0 && (
										<p class="text-xs text-slate-500">
											Aucune donnee pour le moment.
										</p>
									)
								}
							</div>
						</div>
					</div>
				</div>

				<div
					class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div class="flex items-center justify-between">
						<div>
							<p class="text-sm font-semibold text-slate-800">
								CA par mois
							</p>
							<p class="text-xs text-slate-500">
								Vue business sur les 6 derniers mois
							</p>
						</div>
						<span
							class="rounded-full bg-[#eef2ff] px-3 py-1 text-xs font-semibold text-[#5a5dff]"
						>
							6 mois
						</span>
					</div>
					<div class="mt-6 grid gap-4 lg:grid-cols-[1.8fr_1fr]">
						<div class="flex h-52 items-end gap-3">
							{
								monthlyStats.map((stat) => (
									<div class="flex flex-1 flex-col items-center gap-3">
										<div class="w-full rounded-[1.25rem] bg-slate-100/80 p-2">
											<div
												class="w-full rounded-[1rem] bg-gradient-to-t from-[#315eff] via-[#7a4bff] to-[#ff77c8] shadow-lg shadow-indigo-500/25"
												style={`height: ${Math.max(12, (stat.revenue / maxMonthlyRevenue) * 160)}px`}
											/>
										</div>
										<div class="text-center text-xs text-slate-500">
											<p class="font-semibold text-slate-700">
												{stat.label}
											</p>
											<p>{formatNumber(stat.revenue)} EUR</p>
										</div>
									</div>
								))
							}
						</div>
						<div class="space-y-3">
							{
								monthlyStats
									.slice()
									.reverse()
									.slice(0, 3)
									.map((stat) => (
										<div class="rounded-2xl border border-slate-100 bg-slate-50 px-4 py-3">
											<p class="text-xs uppercase tracking-[0.2em] text-slate-400">
												{stat.label}
											</p>
											<p class="mt-1 text-lg font-semibold text-slate-900">
												{formatNumber(stat.revenue)} EUR
											</p>
											<p class="text-xs text-slate-500">
												{formatNumber(stat.clients)} clients actifs
											</p>
										</div>
									))
							}
						</div>
					</div>
				</div>

			</section>

			<section
				data-tab-panel="promos"
				class="relative z-10 hidden flex-col gap-6"
			>
				<div class="admin-section-header">
					<div>
						<p class="admin-eyebrow">Offres commerciales</p>
						<h2>Codes promo</h2>
						<p>Crée, active ou coupe les offres sans les mélanger au pilotage business.</p>
					</div>
					<span>{formatNumber(promoCodes.length)} code(s)</span>
				</div>

				<div class="grid gap-6 xl:grid-cols-[1.1fr_0.9fr]">
					<div
						class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
					>
						<div class="flex flex-col gap-2">
							<p class="text-sm font-semibold text-slate-900">
								Ajouter un code promo
							</p>
							<p class="text-xs text-slate-500">
								Creation rapide d'une offre commerciale depuis l'admin.
							</p>
						</div>
						<form class="mt-6 grid gap-4 md:grid-cols-2" data-promo-form>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">Code</span>
								<input
									type="text"
									name="code"
									required
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 uppercase outline-none transition focus:border-[#7a4bff]"
									placeholder="BIENVENUE5"
								/>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">Type</span>
								<select
									name="type"
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
								>
									<option value="fixed">Montant fixe</option>
									<option value="percent">Pourcentage</option>
								</select>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">Valeur</span>
								<input
									type="number"
									name="value"
									min="1"
									step="1"
									required
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
									placeholder="5"
								/>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">
									Cartons minimum
								</span>
								<input
									type="number"
									name="min_cartons"
									min="1"
									step="1"
									value="1"
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
								/>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">
									Limite globale
								</span>
								<input
									type="number"
									name="max_uses"
									min="1"
									step="1"
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
									placeholder="Optionnel"
								/>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">
									Debut
								</span>
								<input
									type="datetime-local"
									name="starts_at"
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
								/>
							</label>
							<label class="space-y-2 text-sm text-slate-600">
								<span class="font-medium text-slate-800">Fin</span>
								<input
									type="datetime-local"
									name="ends_at"
									class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
								/>
							</label>
							<label class="flex items-center gap-3 rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-medium text-slate-700">
								<input
									type="checkbox"
									name="is_active"
									checked
									class="accent-[#7a4bff]"
								/>
								<span>Activer des la creation</span>
							</label>
							<div class="flex items-end">
								<button
									type="submit"
									class="action-button action-primary w-full justify-center"
								>
									Ajouter le code promo
								</button>
							</div>
						</form>
						<p class="mt-3 text-xs text-slate-500" data-promo-form-status></p>
					</div>

					<div
						class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
					>
						<div class="flex items-center justify-between">
							<div>
								<p class="text-sm font-semibold text-slate-900">
									Codes promo actifs
								</p>
								<p class="text-xs text-slate-500">
									Activation rapide des offres existantes
								</p>
							</div>
							<span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
								{formatNumber(promoCodes.length)} codes
							</span>
						</div>
						<div class="mt-6 space-y-3">
							{promoCodes.map((promo) => (
								<div class="rounded-2xl border border-slate-100 bg-slate-50 px-4 py-4">
									<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
										<div>
											<p class="text-sm font-semibold text-slate-900">
												{promo.code}
											</p>
											<p class="text-xs text-slate-500">
												{promo.type === "percent"
													? `${formatNumber(Number(promo.value || 0))}%`
													: `${formatNumber(Number(promo.value || 0))} EUR`} • min {formatNumber(Number(promo.min_cartons || 1))} carton(s)
											</p>
											<p class="text-xs text-slate-400">
												{promo.max_uses
													? `Limite ${promo.max_uses}`
													: "Sans limite"}
											</p>
										</div>
										<div class="flex items-center gap-2">
											<span class={`badge ${promo.is_active ? "badge-success" : "badge-error"}`}>
												{promo.is_active ? "Actif" : "Inactif"}
											</span>
											<button
												type="button"
												class="filter-button"
												data-action="toggle-promo-status"
												data-promo-id={promo.id}
												data-promo-active={promo.is_active ? "true" : "false"}
											>
												{promo.is_active ? "Desactiver" : "Activer"}
											</button>
										</div>
									</div>
								</div>
							))}
							{promoCodes.length === 0 && (
								<p class="text-sm text-slate-500">
									Aucun code promo cree pour le moment.
								</p>
							)}
						</div>
					</div>
				</div>

			</section>

			<section
				data-tab-panel="dashboard"
				class="relative z-10 flex flex-col gap-8"
			>
				<div
					class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div class="flex items-center justify-between">
						<div>
							<p class="text-sm font-semibold text-slate-800">
								Top 10 des gouts demandes
							</p>
							<p class="text-xs text-slate-500">
								Popularite et tendance recente
							</p>
						</div>
					</div>
					<div class="mt-6 space-y-4">
						{
							flavorStats.slice(0, 10).map((flavor) => {
								const weekCount =
									weekFlavorTotals.get(flavor.id) ?? 0;
								const prevCount =
									prevWeekFlavorTotals.get(flavor.id) ?? 0;
								const trend =
									prevCount === 0
										? weekCount > 0
											? 100
											: 0
										: ((weekCount - prevCount) /
												prevCount) *
											100;
								const trendLabel = `${trend >= 0 ? "+" : ""}${trend.toFixed(0)}%`;
								const progress =
									totalFlavorCount > 0
										? (flavor.total / totalFlavorCount) *
											100
										: 0;
								return (
									<div class="flex flex-col gap-2">
										<div class="flex items-center justify-between text-sm text-slate-700">
											<span class="font-medium">
												{flavor.name}
											</span>
											<span
												class={`trend-badge ${trend >= 0 ? "trend-up" : "trend-down"}`}
											>
												{trendLabel}
											</span>
										</div>
										<div class="flex items-center gap-3 text-xs text-slate-500">
											<span>{flavor.total} votes</span>
											<div class="relative h-2 flex-1 overflow-hidden rounded-full bg-slate-100">
												<div
													class="absolute left-0 top-0 h-full rounded-full bg-gradient-to-r from-[#4f6fff] via-[#7a4bff] to-[#d24bff]"
													style={`width: ${Math.max(6, progress)}%`}
												/>
											</div>
											<span>{progress.toFixed(0)}%</span>
										</div>
									</div>
								);
							})
						}
						{
							flavorStats.length === 0 && (
								<p class="text-sm text-slate-500">
									Aucune commande pour le moment.
								</p>
							)
						}
					</div>
				</div>
			</section>

			<section
				data-tab-panel="orders"
				class="relative z-10 hidden flex-col gap-6"
			>
				<div class="admin-section-header">
					<div>
						<p class="admin-eyebrow">Traitement client</p>
						<h2>Commandes</h2>
						<p>Filtre, inspecte, confirme, livre ou annule les commandes depuis une seule file.</p>
					</div>
					<span>{formatNumber(orders.length)} commande(s)</span>
				</div>

				<div
					class="rounded-3xl bg-white/85 p-5 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div
						class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"
					>
						<div
							class="flex flex-1 items-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600 shadow-sm"
						>
							<svg
								class="h-4 w-4 text-slate-400"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
							>
								<circle cx="11" cy="11" r="7"></circle>
								<path d="M20 20l-3.5-3.5"></path>
							</svg>
							<input
								data-order-search
								type="text"
								placeholder="Rechercher une commande, un nom ou un email"
								class="w-full bg-transparent outline-none"
							/>
						</div>
						<div class="flex flex-wrap gap-2">
							<div class="flex items-center gap-2 rounded-2xl border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600 shadow-sm">
								<span>Precommande</span>
								<select
									data-preorder-filter
									class="bg-transparent pr-2 text-sm font-medium text-slate-700 outline-none"
								>
									<option value="all" selected={defaultOrdersPreorderId === "all"}>
										Toutes
									</option>
									{(preordersData ?? []).map((preorder) => (
										<option
											value={preorder.id}
											selected={defaultOrdersPreorderId === preorder.id}
										>
											{preorder.name} {preorder.status === "open" ? "• ouverte" : ""}
										</option>
									))}
								</select>
							</div>
							<a
								href="/api/admin/export-orders.csv"
								class="filter-button inline-flex items-center"
							>
								Exporter CSV
							</a>
							<button
								class="filter-button filter-active"
								data-status-filter="all"
								type="button"
							>
								Tous
							</button>
							<button
								class="filter-button"
								data-status-filter="pending"
								type="button"
							>
								En attente
							</button>
							<button
								class="filter-button"
								data-status-filter="confirmed"
								type="button"
							>
								Confirmees
							</button>
							<button
								class="filter-button"
								data-status-filter="delivered"
								type="button"
							>
								Livrees
							</button>
						</div>
					</div>
				</div>

				<div class="space-y-4" data-order-list>
					{
						orders.map((order) => {
							const user = userMap.get(order.user_id) ?? {
								email: "",
								firstName: "",
								lastName: "",
							};
							const fullName =
								[user.firstName, user.lastName]
									.filter(Boolean)
									.join(" ") || "Client";
							const orderNumber =
								order.order_number ||
								`CMD-${order.id.slice(0, 8)}`;
							const statusLabel =
								{
									pending: "En attente",
									confirmed: "Confirmee",
									delivered: "Livree",
									cancelled: "Annulee",
								}[order.status as string] || "En attente";
							const statusClass =
								{
									pending: "badge-warning",
									confirmed: "badge-info",
									delivered: "badge-success",
									cancelled: "badge-error",
								}[order.status as string] || "badge-warning";
							const flavorCount =
								flavorCountByOrder.get(order.id) ?? 0;
							const orderFlavorsList =
								flavorsByOrder.get(order.id) ?? [];
							const preorderName =
								preordersData?.find((preorder) => preorder.id === order.preorder_id)?.name ||
								"Precommande inconnue";
							const searchValue =
								`${orderNumber} ${fullName} ${user.email} ${preorderName}`.toLowerCase();
							const purchaseCost = order.cartons * COST_PER_CARTON;
							const margin = order.total - purchaseCost;
							const paymentProofUrl = paymentProofUrls.get(order.id) || "";
							const marginPercent =
								order.total > 0
									? (margin / order.total) * 100
									: 0;
							return (
								<div
									class="order-card rounded-3xl bg-white/85 p-5 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
									data-order-card
									data-status={order.status}
									data-preorder-id={order.preorder_id || ""}
									data-search={searchValue}
								>
									<div class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
										<div class="flex items-center gap-4">
											<div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-[#eef2ff] text-lg">
												<svg
													class="h-5 w-5 text-[#5a5dff]"
													viewBox="0 0 24 24"
													fill="none"
													stroke="currentColor"
													stroke-width="2"
													stroke-linecap="round"
													stroke-linejoin="round"
												>
													<path d="M21 8v8l-9 5-9-5V8l9-5 9 5z" />
													<path d="M12 3v18" />
													<path d="M3 8l9 5 9-5" />
												</svg>
											</div>
											<div>
												<p class="text-sm font-semibold text-slate-900">
													{orderNumber}
												</p>
												<p class="text-xs text-slate-500">
													{fullName}
												</p>
												<p class="text-xs text-slate-400">
													{preorderName}
												</p>
												<p class="text-xs text-slate-400">
													{formatDate(
														order.created_at,
													)}
												</p>
											</div>
										</div>
										<div class="flex flex-wrap items-center gap-3 text-xs text-slate-500">
											<span
												class={`badge ${statusClass}`}
											>
												{statusLabel}
											</span>
											<span>{order.cartons} cartons</span>
											<span>
												{formatNumber(order.total)} EUR
											</span>
											<span>
												Marge {formatNumber(margin)} EUR
											</span>
											<span>
												{marginPercent.toFixed(1)}%
											</span>
										</div>
										<button
											type="button"
											class="toggle-button"
											data-action="toggle-order"
											aria-expanded="false"
										>
											<svg
												class="h-4 w-4"
												viewBox="0 0 24 24"
												fill="none"
												stroke="currentColor"
												stroke-width="2"
												stroke-linecap="round"
												stroke-linejoin="round"
											>
												<path d="M6 9l6 6 6-6" />
											</svg>
										</button>
									</div>

									<div
										class="mt-4 hidden space-y-4 border-t border-slate-100 pt-4"
										data-order-details
									>
										<div class="grid gap-3 text-sm text-slate-600 sm:grid-cols-2">
											<div>
												<p class="text-xs text-slate-400">
													Email
												</p>
												<p>
													{user.email ||
														"non renseigne"}
												</p>
											</div>
											<div>
												<p class="text-xs text-slate-400">
													Mode de paiement
												</p>
												<p>
													{order.payment_method ||
														"--"}
												</p>
											</div>
											<div>
												<p class="text-xs text-slate-400">
													Preuve paiement
												</p>
												{paymentProofUrl ? (
													<div class="mt-2 flex items-center gap-3">
														<a
															href={paymentProofUrl}
															target="_blank"
															rel="noreferrer"
															class="block overflow-hidden rounded-xl border border-black/10 bg-white"
														>
															<img
																src={paymentProofUrl}
																alt="Preuve de paiement"
																class="h-20 w-28 object-cover"
																loading="lazy"
															/>
														</a>
														<a
															href={paymentProofUrl}
															target="_blank"
															rel="noreferrer"
															class="font-semibold text-[#116b4c] underline decoration-[#28e08f]/50 underline-offset-4"
														>
															Ouvrir
														</a>
													</div>
												) : (
													<p class="font-semibold text-red-600">
														Manquante
													</p>
												)}
											</div>
											<div>
												<p class="text-xs text-slate-400">
													Gouts
												</p>
												<p>{flavorCount} selections</p>
											</div>
											<div>
												<p class="text-xs text-slate-400">
													Cout achat
												</p>
												<p>
													{formatNumber(
														purchaseCost,
													)}{" "}
													EUR
												</p>
											</div>
										</div>

										<div class="flex flex-wrap gap-2">
											{orderFlavorsList.map((flavor) => (
												<span class="flavor-pill">
													{flavor.name} x
													{flavor.quantity}
												</span>
											))}
											{orderFlavorsList.length === 0 && (
												<span class="text-xs text-slate-500">
													Aucun gout associe.
												</span>
											)}
										</div>

										<div class="flex flex-wrap gap-3">
											{order.status === "pending" && (
												<button
													class="action-button action-primary"
													data-action="update-status"
													data-order-id={order.id}
													data-next-status="confirmed"
													type="button"
												>
													Confirmer
												</button>
											)}
											{order.status === "confirmed" && (
												<button
													class="action-button action-success"
													data-action="update-status"
													data-order-id={order.id}
													data-next-status="delivered"
													type="button"
												>
													Marquer comme livree
												</button>
											)}
											<button
												class="action-button action-danger"
												data-action="update-status"
												data-order-id={order.id}
												data-next-status="cancelled"
												type="button"
											>
												Annuler
											</button>
										</div>
									</div>
								</div>
							);
						})
					}
					{
						orders.length === 0 && (
							<div class="rounded-3xl bg-white/85 p-6 text-sm text-slate-500 shadow-lg shadow-slate-200/60 ring-1 ring-white/50">
								Aucune commande pour le moment.
							</div>
						)
					}
				</div>
			</section>

			<section
				data-tab-panel="preorders"
				class="relative z-10 hidden flex-col gap-6"
			>
				<div class="admin-section-header">
					<div>
						<p class="admin-eyebrow">Cycle de vente</p>
						<h2>Précommandes</h2>
						<p>Ouvre une vague, annonce aux clients, clôture, prépare le fournisseur puis notifie le stock reçu.</p>
					</div>
					<span>{openPreorder ? "Vague ouverte" : "Aucune vague ouverte"}</span>
				</div>

				<div class="grid gap-4 md:grid-cols-3">
					<div class="metric-card">
						<p class="metric-label">Precommandes actives</p>
						<p class="metric-value">{formatNumber(openClients)}</p>
						<p class="metric-sub">Clients uniques</p>
					</div>
					<div class="metric-card">
						<p class="metric-label">Cartons a commander</p>
						<p class="metric-value">{formatNumber(openCartons)}</p>
						<p class="metric-sub">Total actuel</p>
					</div>
					<div
						class="metric-card"
						data-countdown
						data-end-date={openEndDate?.toISOString()}
					>
						<p class="metric-label">Temps restant</p>
						<p class="metric-value">{openRemainingLabel}</p>
						<p class="metric-sub">
							{
								openPreorder
									? openPreorder.name
									: "Aucune ouverte"
							}
						</p>
					</div>
				</div>

				<div class="admin-action-board">
					<div>
						<p class="admin-action-title">Communication</p>
						<p class="admin-action-sub">Prévenir les clients au bon moment.</p>
						<button
							type="button"
							class="action-button action-primary"
							data-action="send-open-email"
							data-preorder-id={openPreorder?.id || ""}
							disabled={!openPreorder}
						>
							Annoncer l'ouverture
						</button>
					</div>
					<div>
						<p class="admin-action-title">Fenêtre de vente</p>
						<p class="admin-action-sub">Ajuster la vague active.</p>
						<div class="flex flex-wrap gap-2">
							<button
								type="button"
								class="action-button action-danger"
								data-action="close-preorder"
								disabled={!openPreorder}
							>
								Clôturer
							</button>
							<button
								type="button"
								class="action-button action-primary"
								data-action="extend-preorder"
								disabled={!openPreorder}
							>
								+24h
							</button>
						</div>
					</div>
					<div>
						<p class="admin-action-title">Après clôture</p>
						<p class="admin-action-sub">Informer sur fournisseur et disponibilité.</p>
						<div class="flex flex-wrap gap-2">
							<button
								type="button"
								class="action-button action-primary"
								data-action="send-supplier-email"
								data-preorder-id={openPreorder?.id || ""}
								disabled={!openPreorder}
							>
								Fournisseur envoyé
							</button>
							<button
								type="button"
								class="action-button action-success"
								data-action="send-stock-email"
								data-preorder-id={openPreorder?.id || ""}
								disabled={!openPreorder}
							>
								Stock reçu
							</button>
						</div>
					</div>
				</div>
				<p class="text-xs text-slate-500" data-admin-email-status></p>

				<div
					class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div class="flex flex-col gap-2">
						<p class="text-sm font-semibold text-slate-900">
							Creer une precommande
						</p>
						<p class="text-xs text-slate-500">
							Nom, dates et statut initial pour lancer la prochaine
							fenetre.
						</p>
					</div>
					<form class="mt-6 grid gap-4 lg:grid-cols-2" data-preorder-form>
						<label class="space-y-2 text-sm text-slate-600 lg:col-span-2">
							<span class="font-medium text-slate-800">Nom</span>
							<input
								type="text"
								name="name"
								required
								class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
								placeholder="Precommande Avril 2026"
							/>
						</label>
						<label class="space-y-2 text-sm text-slate-600">
							<span class="font-medium text-slate-800">
								Date de debut
							</span>
							<input
								type="datetime-local"
								name="start_date"
								required
								class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
							/>
						</label>
						<label class="space-y-2 text-sm text-slate-600">
							<span class="font-medium text-slate-800">Date de fin</span>
							<input
								type="datetime-local"
								name="end_date"
								required
								class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
							/>
						</label>
						<label class="space-y-2 text-sm text-slate-600">
							<span class="font-medium text-slate-800">
								Statut initial
							</span>
							<select
								name="status"
								class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 outline-none transition focus:border-[#7a4bff]"
							>
								<option value="planned">Planifiee</option>
								<option value="open">Ouverte</option>
								<option value="closed">Fermee</option>
							</select>
						</label>
						<div class="flex items-end">
							<button
								type="submit"
								class="action-button action-primary w-full justify-center"
							>
								Creer la precommande
							</button>
						</div>
					</form>
					<p class="mt-3 text-xs text-slate-500" data-preorder-form-status></p>
				</div>

				<div
					class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div class="flex items-center justify-between">
						<div>
							<p class="text-sm font-semibold text-slate-900">
								Calendrier des precommandes
							</p>
							<p class="text-xs text-slate-500">
								Ouvrir, fermer ou prevoir les prochaines fenetres.
							</p>
						</div>
						<span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
							{formatNumber((preordersData ?? []).length)} fenetres
						</span>
					</div>
					<div class="mt-6 space-y-3">
						{(preordersData ?? []).map((preorder) => {
							const statusLabel =
								preorder.status === "open"
									? "Ouverte"
									: preorder.status === "planned"
										? "Planifiee"
										: "Fermee";
							const badgeClass =
								preorder.status === "open"
									? "badge-info"
									: preorder.status === "planned"
										? "badge-warning"
										: "badge-error";
							return (
								<div class="rounded-2xl border border-slate-100 bg-slate-50 px-4 py-4">
									<div class="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between">
										<div>
											<p class="text-sm font-semibold text-slate-800">
												{preorder.name}
											</p>
											<p class="text-xs text-slate-500">
												{formatDate(preorder.start_date)} au {formatDate(preorder.end_date)}
											</p>
										</div>
										<div class="flex flex-wrap items-center gap-2">
											<span class={`badge ${badgeClass}`}>{statusLabel}</span>
											{preorder.status !== "open" && (
												<button
													type="button"
													class="filter-button"
													data-action="set-preorder-status"
													data-preorder-id={preorder.id}
													data-next-status="open"
												>
													Ouvrir
												</button>
											)}
											{preorder.status !== "planned" && (
												<button
													type="button"
													class="filter-button"
													data-action="set-preorder-status"
													data-preorder-id={preorder.id}
													data-next-status="planned"
												>
													Planifier
												</button>
											)}
											{preorder.status !== "closed" && (
												<button
													type="button"
													class="filter-button"
													data-action="set-preorder-status"
													data-preorder-id={preorder.id}
													data-next-status="closed"
												>
													Fermer
												</button>
											)}
										</div>
									</div>
								</div>
							);
						})}
						{(preordersData ?? []).length === 0 && (
							<p class="text-sm text-slate-500">
								Aucune precommande enregistree pour le moment.
							</p>
						)}
					</div>
				</div>

				<div
					class="rounded-3xl bg-white/85 p-6 shadow-lg shadow-slate-200/60 ring-1 ring-white/50"
				>
					<div class="flex items-center justify-between">
						<div>
							<p class="text-sm font-semibold text-slate-900">
								Calcul de l'arrivage
							</p>
							<p class="text-xs text-slate-500">
								Repartition des gouts demandes
							</p>
						</div>
					</div>
					<div class="mt-6 space-y-4">
						{
							openFlavorStats.map((item) => {
								const percent =
									openOrders.length > 0
										? (item.orders / openOrders.length) *
											100
										: 0;
								return (
									<div class="rounded-2xl border border-slate-100 bg-slate-50 px-4 py-3">
										<p class="text-sm font-semibold text-slate-800">
											{item.name}
										</p>
										<div class="mt-1 flex items-center gap-3 text-xs text-slate-500">
											<span>{item.quantity} cartons</span>
											<span>
												Demande par {percent.toFixed(0)}
												% des clients
											</span>
										</div>
									</div>
								);
							})
						}
						{
							openFlavorStats.length === 0 && (
								<p class="text-sm text-slate-500">
									Aucune demande pour cette precommande.
								</p>
							)
						}
					</div>
				</div>
			</section>
		</div>
	</main>
</Layout>

<script>
	const tabButtons = Array.from(
		document.querySelectorAll("[data-tab-target]"),
	);
	const tabPanels = Array.from(document.querySelectorAll("[data-tab-panel]"));
	const tabButtonClass = "tab-active";

	const activateTab = (target) => {
		tabButtons.forEach((button) => {
			const isActive = button.dataset.tabTarget === target;
			button.classList.toggle(tabButtonClass, isActive);
		});
		tabPanels.forEach((panel) => {
			panel.classList.toggle("hidden", panel.dataset.tabPanel !== target);
		});
	};

	tabButtons.forEach((button) => {
		button.addEventListener("click", () =>
			activateTab(button.dataset.tabTarget),
		);
	});

	const filterButtons = Array.from(
		document.querySelectorAll("[data-status-filter]"),
	);
	const searchInput = document.querySelector("[data-order-search]");
	const preorderFilter = document.querySelector("[data-preorder-filter]");
	const orderCards = Array.from(
		document.querySelectorAll("[data-order-card]"),
	);
	const emailStatus = document.querySelector("[data-admin-email-status]");
	const preorderForm = document.querySelector("[data-preorder-form]");
	const preorderFormStatus = document.querySelector(
		"[data-preorder-form-status]",
	);
	const promoForm = document.querySelector("[data-promo-form]");
	const promoFormStatus = document.querySelector("[data-promo-form-status]");
	let activeStatus = "all";
	let activePreorder =
		preorderFilter instanceof HTMLSelectElement
			? preorderFilter.value || "all"
			: "all";

	const applyFilters = () => {
		const query = (searchInput?.value || "").trim().toLowerCase();
		orderCards.forEach((card) => {
			const status = card.dataset.status || "";
			const search = card.dataset.search || "";
			const preorderId = card.dataset.preorderId || "";
			const statusMatch =
				activeStatus === "all" || status === activeStatus;
			const preorderMatch =
				activePreorder === "all" || preorderId === activePreorder;
			const queryMatch = !query || search.includes(query);
			card.classList.toggle(
				"hidden",
				!(statusMatch && preorderMatch && queryMatch),
			);
		});
	};

	filterButtons.forEach((button) => {
		button.addEventListener("click", () => {
			activeStatus = button.dataset.statusFilter || "all";
			filterButtons.forEach((btn) =>
				btn.classList.toggle("filter-active", btn === button),
			);
			applyFilters();
		});
	});

	if (searchInput) {
		searchInput.addEventListener("input", applyFilters);
	}

	if (preorderFilter instanceof HTMLSelectElement) {
		preorderFilter.addEventListener("change", () => {
			activePreorder = preorderFilter.value || "all";
			applyFilters();
		});
	}

	applyFilters();

	if (preorderForm instanceof HTMLFormElement) {
		preorderForm.addEventListener("submit", async (event) => {
			event.preventDefault();
			const formData = new FormData(preorderForm);
			const submitButton = preorderForm.querySelector(
				"button[type='submit']",
			);
			if (submitButton instanceof HTMLButtonElement) {
				submitButton.disabled = true;
				submitButton.textContent = "Creation...";
			}
			if (preorderFormStatus) {
				preorderFormStatus.textContent =
					"Creation de la precommande en cours...";
			}
			try {
				const response = await fetch("/api/admin/create-preorder", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({
						name: formData.get("name"),
						start_date: formData.get("start_date"),
						end_date: formData.get("end_date"),
						status: formData.get("status"),
					}),
				});
				const result = await response.json();
				if (!response.ok) {
					throw new Error(result?.error || result?.message || "Erreur");
				}
				if (preorderFormStatus) {
					preorderFormStatus.textContent =
						"Precommande creee. Rechargement...";
				}
				window.location.reload();
			} catch (error) {
				if (preorderFormStatus) {
					preorderFormStatus.textContent =
						"Impossible de creer la precommande.";
				}
				console.error(error);
				if (submitButton instanceof HTMLButtonElement) {
					submitButton.disabled = false;
					submitButton.textContent = "Creer la precommande";
				}
			}
		});
	}

	if (promoForm instanceof HTMLFormElement) {
		promoForm.addEventListener("submit", async (event) => {
			event.preventDefault();
			const formData = new FormData(promoForm);
			const submitButton = promoForm.querySelector("button[type='submit']");
			if (submitButton instanceof HTMLButtonElement) {
				submitButton.disabled = true;
				submitButton.textContent = "Creation...";
			}
			if (promoFormStatus) {
				promoFormStatus.textContent = "Creation du code promo en cours...";
			}
			try {
				const response = await fetch("/api/admin/create-promo-code", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({
						code: formData.get("code"),
						type: formData.get("type"),
						value: formData.get("value"),
						min_cartons: formData.get("min_cartons"),
						max_uses: formData.get("max_uses"),
						starts_at: formData.get("starts_at"),
						ends_at: formData.get("ends_at"),
						is_active: formData.get("is_active") === "on"
					})
				});
				const result = await response.json();
				if (!response.ok) {
					throw new Error(result?.error || "Erreur");
				}
				if (promoFormStatus) {
					promoFormStatus.textContent = `Code ${result?.promo?.code || ""} cree.`;
				}
				window.location.reload();
			} catch (error) {
				if (promoFormStatus) {
					promoFormStatus.textContent = "Impossible de creer le code promo.";
				}
				console.error(error);
				if (submitButton instanceof HTMLButtonElement) {
					submitButton.disabled = false;
					submitButton.textContent = "Ajouter le code promo";
				}
			}
		});
	}

	document.addEventListener("click", async (event) => {
		const target = event.target;
		if (!(target instanceof HTMLElement)) return;

		const toggleButton = target.closest("[data-action='toggle-order']");
		if (toggleButton) {
			const card = toggleButton.closest("[data-order-card]");
			const details = card?.querySelector("[data-order-details]");
			if (details) {
				details.classList.toggle("hidden");
				toggleButton.classList.toggle("rotate-180");
			}
			return;
		}

		const actionButton = target.closest("[data-action='update-status']");
		if (actionButton) {
			const orderId = actionButton.getAttribute("data-order-id");
			const nextStatus = actionButton.getAttribute("data-next-status");
			if (!orderId || !nextStatus) return;
			const originalText = actionButton.textContent || "";
			actionButton.setAttribute("disabled", "true");
			actionButton.textContent = "Mise a jour...";
			try {
				const response = await fetch("/api/admin/update-order-status", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({
						order_id: orderId,
						status: nextStatus,
					}),
				});
				if (!response.ok) {
					throw new Error(await response.text());
				}
				window.location.reload();
			} catch (err) {
				actionButton.removeAttribute("disabled");
				actionButton.textContent = originalText;
				console.error(err);
			}
			return;
		}

		const closeButton = target.closest("[data-action='close-preorder']");
		if (closeButton) {
			closeButton.setAttribute("disabled", "true");
			closeButton.textContent = "Cloture...";
			try {
				const response = await fetch("/api/admin/close-preorder", {
					method: "POST",
				});
				if (!response.ok) throw new Error(await response.text());
				window.location.reload();
			} catch (err) {
				closeButton.removeAttribute("disabled");
				closeButton.textContent = "Cloturer les precommandes";
				console.error(err);
			}
			return;
		}

		const extendButton = target.closest("[data-action='extend-preorder']");
		if (extendButton) {
			extendButton.setAttribute("disabled", "true");
			extendButton.textContent = "Prolongation...";
			try {
				const response = await fetch("/api/admin/extend-preorder", {
					method: "POST",
				});
				if (!response.ok) throw new Error(await response.text());
				window.location.reload();
			} catch (err) {
				extendButton.removeAttribute("disabled");
				extendButton.textContent = "Prolonger de 24h";
				console.error(err);
			}
		}

		const supplierButton = target.closest("[data-action='send-supplier-email']");
		if (supplierButton) {
			const preorderId = supplierButton.getAttribute("data-preorder-id") || "";
			if (!preorderId) return;
			supplierButton.setAttribute("disabled", "true");
			supplierButton.textContent = "Envoi...";
			if (emailStatus) emailStatus.textContent = "Envoi des emails fournisseur...";
			try {
				const response = await fetch("/api/admin/preorder/send-supplier-order-emails", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({ preorderId })
				});
				const result = await response.json();
				if (!response.ok) throw new Error(result?.error || "Erreur");
				if (emailStatus) emailStatus.textContent = `Emails envoyes: ${result?.sent ?? 0}`;
			} catch (err) {
				if (emailStatus) emailStatus.textContent = "Erreur lors de l'envoi des emails.";
				console.error(err);
			} finally {
				supplierButton.removeAttribute("disabled");
				supplierButton.textContent = "Commande fournisseur envoyee";
			}
			return;
		}

		const stockButton = target.closest("[data-action='send-stock-email']");
		if (stockButton) {
			const preorderId = stockButton.getAttribute("data-preorder-id") || "";
			if (!preorderId) return;
			stockButton.setAttribute("disabled", "true");
			stockButton.textContent = "Envoi...";
			if (emailStatus) emailStatus.textContent = "Envoi des emails stock recu...";
			try {
				const response = await fetch("/api/admin/preorder/send-stock-received-emails", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({ preorderId })
				});
				const result = await response.json();
				if (!response.ok) throw new Error(result?.error || "Erreur");
				if (emailStatus) emailStatus.textContent = `Emails envoyes: ${result?.sent ?? 0}`;
			} catch (err) {
				if (emailStatus) emailStatus.textContent = "Erreur lors de l'envoi des emails.";
				console.error(err);
			} finally {
				stockButton.removeAttribute("disabled");
				stockButton.textContent = "Stock recu / pret a livrer";
			}
		}

		const openEmailButton = target.closest("[data-action='send-open-email']");
		if (openEmailButton) {
			const preorderId =
				openEmailButton.getAttribute("data-preorder-id") || "";
			if (!preorderId) return;
			openEmailButton.setAttribute("disabled", "true");
			openEmailButton.textContent = "Envoi...";
			if (emailStatus) {
				emailStatus.textContent =
					"Envoi de l'annonce d'ouverture aux clients...";
			}
			try {
				const response = await fetch("/api/admin/preorder/send-open-emails", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({ preorderId }),
				});
				const result = await response.json();
				if (!response.ok) throw new Error(result?.error || "Erreur");
				if (emailStatus) {
					emailStatus.textContent = `Annonce envoyee a ${result?.sent ?? 0} clients.`;
				}
			} catch (error) {
				if (emailStatus) {
					emailStatus.textContent =
						"Erreur lors de l'envoi du mail d'ouverture.";
				}
				console.error(error);
			} finally {
				openEmailButton.removeAttribute("disabled");
				openEmailButton.textContent = "Annoncer l'ouverture par email";
			}
			return;
		}

		const preorderStatusButton = target.closest(
			"[data-action='set-preorder-status']",
		);
		if (preorderStatusButton) {
			const preorderId =
				preorderStatusButton.getAttribute("data-preorder-id") || "";
			const status =
				preorderStatusButton.getAttribute("data-next-status") || "";
			if (!preorderId || !status) return;
			preorderStatusButton.setAttribute("disabled", "true");
			const initialText = preorderStatusButton.textContent || "";
			preorderStatusButton.textContent = "Mise a jour...";
			try {
				const response = await fetch("/api/admin/update-preorder-status", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({ preorderId, status }),
				});
				if (!response.ok) {
					throw new Error(await response.text());
				}
				window.location.reload();
			} catch (error) {
				preorderStatusButton.removeAttribute("disabled");
				preorderStatusButton.textContent = initialText;
				console.error(error);
			}
			return;
		}

		const togglePromoButton = target.closest(
			"[data-action='toggle-promo-status']",
		);
		if (togglePromoButton) {
			const promoId = togglePromoButton.getAttribute("data-promo-id") || "";
			const isActive =
				togglePromoButton.getAttribute("data-promo-active") === "true";
			if (!promoId) return;
			togglePromoButton.setAttribute("disabled", "true");
			const initialText = togglePromoButton.textContent || "";
			togglePromoButton.textContent = "Mise a jour...";
			try {
				const response = await fetch("/api/admin/update-promo-code", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify({
						promoId,
						is_active: !isActive
					})
				});
				if (!response.ok) {
					throw new Error(await response.text());
				}
				window.location.reload();
			} catch (error) {
				togglePromoButton.removeAttribute("disabled");
				togglePromoButton.textContent = initialText;
				console.error(error);
			}
			return;
		}
	});

	const countdownEl = document.querySelector("[data-countdown]");
	if (countdownEl) {
		const endDate = countdownEl.getAttribute("data-end-date");
		if (endDate) {
			const endTime = new Date(endDate).getTime();
			const valueEl = countdownEl.querySelector(".metric-value");
			const tick = () => {
				const diff = endTime - Date.now();
				if (!valueEl) return;
				if (diff <= 0) {
					valueEl.textContent = "Terminee";
					return;
				}
				const hours = Math.floor(diff / 3600000);
				const minutes = Math.floor((diff % 3600000) / 60000);
				valueEl.textContent = `${String(hours).padStart(2, "0")}h ${String(
					minutes,
				).padStart(2, "0")}m`;
			};
			tick();
			setInterval(tick, 1000);
		}
	}
</script>

<style>
	.admin-shell {
		background:
			linear-gradient(180deg, rgba(255, 253, 247, 0.98), rgba(246, 244, 238, 1)),
			linear-gradient(135deg, rgba(40, 224, 143, 0.08), transparent 34%),
			linear-gradient(315deg, rgba(36, 87, 255, 0.08), transparent 28%);
		color: #101820;
	}

	.admin-hero {
		display: grid;
		gap: 1rem;
		align-items: stretch;
		border: 1px solid rgba(16, 24, 32, 0.1);
		border-radius: 2rem;
		background: rgba(255, 253, 247, 0.86);
		padding: 1.25rem;
		box-shadow: 0 24px 70px rgba(16, 24, 32, 0.08);
		backdrop-filter: blur(18px);
	}

	@media (min-width: 768px) {
		.admin-hero {
			grid-template-columns: 1fr minmax(260px, 360px);
			padding: 1.6rem;
		}
	}

	.admin-eyebrow {
		font-size: 0.72rem;
		font-weight: 900;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: #116b4c;
	}

	.admin-title {
		margin-top: 0.35rem;
		font-size: clamp(2.3rem, 5vw, 4.4rem);
		font-weight: 950;
		line-height: 0.92;
		letter-spacing: -0.04em;
		color: #101820;
	}

	.admin-subtitle {
		margin-top: 0.85rem;
		max-width: 48rem;
		font-size: 0.95rem;
		line-height: 1.7;
		color: #64706f;
	}

	.admin-hero-card {
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		border-radius: 1.5rem;
		background: #101820;
		padding: 1.15rem;
		color: white;
	}

	.admin-hero-label {
		font-size: 0.68rem;
		font-weight: 900;
		letter-spacing: 0.16em;
		text-transform: uppercase;
		color: #28e08f;
	}

	.admin-hero-value {
		margin-top: 0.8rem;
		overflow-wrap: anywhere;
		font-size: 0.95rem;
		font-weight: 800;
		color: rgba(255, 255, 255, 0.82);
	}

	.admin-logout,
	.admin-tabs-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 999px;
		font-size: 0.78rem;
		font-weight: 900;
		transition:
			transform 0.18s ease,
			background-color 0.18s ease,
			color 0.18s ease,
			border-color 0.18s ease;
	}

	.admin-logout {
		margin-top: 1.25rem;
		width: fit-content;
		background: rgba(255, 255, 255, 0.1);
		padding: 0.65rem 1rem;
		color: white;
	}

	.admin-logout:hover,
	.admin-tabs-link:hover {
		transform: translateY(-2px);
	}

	.admin-tabs {
		position: sticky;
		top: 4.35rem;
		z-index: 40;
		display: flex;
		flex-wrap: wrap;
		gap: 0.45rem;
		border: 1px solid rgba(16, 24, 32, 0.1);
		border-radius: 999px;
		background: rgba(255, 253, 247, 0.9);
		padding: 0.45rem;
		box-shadow: 0 18px 40px rgba(16, 24, 32, 0.08);
		backdrop-filter: blur(18px);
	}

	.tab-button {
		position: relative;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 999px;
		padding: 0.75rem 1.2rem;
		font-size: 0.8rem;
		font-weight: 900;
		color: #64706f;
		transition:
			transform 0.18s ease,
			background-color 0.18s ease,
			color 0.18s ease;
	}

	.tab-button:hover {
		transform: translateY(-1px);
		color: #101820;
	}

	.tab-active {
		background: #101820;
		color: white;
		box-shadow: 0 12px 28px rgba(16, 24, 32, 0.18);
	}

	.admin-tabs-link {
		margin-left: auto;
		border: 1px solid rgba(16, 24, 32, 0.12);
		padding: 0.72rem 1.1rem;
		color: #64706f;
	}

	.admin-tabs-link:hover {
		border-color: rgba(16, 24, 32, 0.32);
		color: #101820;
	}

	.admin-section-header {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		justify-content: space-between;
		border: 1px solid rgba(16, 24, 32, 0.1);
		border-radius: 1.7rem;
		background: #101820;
		padding: 1.25rem;
		color: white;
		box-shadow: 0 24px 60px rgba(16, 24, 32, 0.16);
	}

	@media (min-width: 768px) {
		.admin-section-header {
			flex-direction: row;
			align-items: end;
			padding: 1.45rem;
		}
	}

	.admin-section-header .admin-eyebrow {
		color: #28e08f;
	}

	.admin-section-header h2 {
		margin-top: 0.35rem;
		font-size: clamp(1.8rem, 3vw, 2.8rem);
		font-weight: 950;
		line-height: 1;
		letter-spacing: -0.04em;
	}

	.admin-section-header p:not(.admin-eyebrow) {
		margin-top: 0.55rem;
		max-width: 48rem;
		font-size: 0.9rem;
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.65);
	}

	.admin-section-header > span {
		width: fit-content;
		border-radius: 999px;
		background: rgba(40, 224, 143, 0.16);
		padding: 0.65rem 1rem;
		font-size: 0.78rem;
		font-weight: 950;
		color: #28e08f;
	}

	.admin-action-board {
		display: grid;
		gap: 1rem;
		grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
	}

	.admin-action-board > div {
		border: 1px solid rgba(16, 24, 32, 0.1);
		border-radius: 1.4rem;
		background: rgba(255, 253, 247, 0.88);
		padding: 1rem;
		box-shadow: 0 18px 48px rgba(16, 24, 32, 0.08);
	}

	.admin-action-title {
		font-size: 0.95rem;
		font-weight: 950;
		color: #101820;
	}

	.admin-action-sub {
		margin: 0.25rem 0 1rem;
		font-size: 0.78rem;
		font-weight: 700;
		line-height: 1.5;
		color: #64706f;
	}

	.stat-card,
	.metric-card,
	.order-card,
	[data-tab-panel] > div[class*="rounded-3xl"],
	[data-tab-panel] > div[class*="rounded-2xl"] {
		border: 1px solid rgba(16, 24, 32, 0.1) !important;
		background: rgba(255, 253, 247, 0.88) !important;
		box-shadow: 0 18px 48px rgba(16, 24, 32, 0.08) !important;
		backdrop-filter: blur(18px);
	}

	.stat-card,
	.metric-card {
		display: flex;
		flex-direction: column;
		gap: 0.45rem;
		border-radius: 1.35rem;
		padding: 1.2rem;
		transition:
			transform 0.2s ease,
			box-shadow 0.2s ease,
			border-color 0.2s ease;
	}

	.stat-card:hover,
	.metric-card:hover,
	.order-card:hover {
		transform: translateY(-3px);
		border-color: rgba(40, 224, 143, 0.5) !important;
		box-shadow: 0 26px 65px rgba(16, 24, 32, 0.12) !important;
	}

	.stat-label,
	.metric-label {
		font-size: 0.68rem;
		font-weight: 950;
		text-transform: uppercase;
		letter-spacing: 0.16em;
		color: #116b4c !important;
	}

	.stat-value,
	.metric-value {
		margin-top: 0.1rem;
		font-size: clamp(1.65rem, 3vw, 2.15rem);
		font-weight: 950;
		line-height: 1;
		letter-spacing: -0.04em;
		color: #101820;
	}

	.stat-sub,
	.metric-sub {
		font-size: 0.78rem;
		font-weight: 700;
		color: #64706f;
	}

	.filter-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 999px;
		border: 1px solid rgba(16, 24, 32, 0.12);
		background: rgba(255, 255, 255, 0.62);
		padding: 0.62rem 1rem;
		font-size: 0.75rem;
		font-weight: 900;
		color: #64706f;
		transition:
			transform 0.18s ease,
			border-color 0.18s ease,
			background-color 0.18s ease,
			color 0.18s ease;
	}

	.filter-button:hover {
		transform: translateY(-2px);
		border-color: rgba(16, 24, 32, 0.3);
		background: white;
		color: #101820;
	}

	.filter-active {
		border-color: #101820;
		background: #101820;
		color: white;
	}

	.action-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 999px;
		padding: 0.72rem 1.15rem;
		font-size: 0.75rem;
		font-weight: 950;
		transition:
			transform 0.18s ease,
			box-shadow 0.18s ease,
			background-color 0.18s ease;
	}

	.action-button:hover {
		transform: translateY(-2px);
	}

	.action-button:disabled {
		opacity: 0.6;
		cursor: not-allowed;
	}

	.action-primary {
		background: #101820;
		color: white;
		box-shadow: 0 14px 28px rgba(16, 24, 32, 0.2);
	}

	.action-success {
		background: #28e08f;
		color: #101820;
		box-shadow: 0 14px 28px rgba(40, 224, 143, 0.2);
	}

	.action-danger {
		background: #e5484d;
		color: white;
		box-shadow: 0 14px 28px rgba(229, 72, 77, 0.2);
	}

	.badge,
	.trend-badge,
	.flavor-pill {
		border-radius: 999px;
		padding: 0.38rem 0.78rem;
		font-size: 0.68rem;
		font-weight: 950;
		letter-spacing: 0.02em;
	}

	.badge-warning {
		background: rgba(245, 158, 11, 0.14);
		color: #9a5b00;
	}

	.badge-info {
		background: rgba(36, 87, 255, 0.12);
		color: #1741b9;
	}

	.badge-success,
	.trend-up {
		background: rgba(40, 224, 143, 0.18);
		color: #116b4c;
	}

	.badge-error,
	.trend-down {
		background: rgba(229, 72, 77, 0.12);
		color: #b4232a;
	}

	.flavor-pill {
		background: rgba(16, 24, 32, 0.06);
		color: #475569;
	}

	.toggle-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		height: 2.35rem;
		width: 2.35rem;
		border-radius: 999px;
		background: #101820;
		color: white;
		transition:
			transform 0.2s ease,
			background-color 0.2s ease;
	}

	.toggle-button:hover {
		transform: translateY(-2px);
		background: #000;
	}

	input,
	select {
		accent-color: #28e08f;
	}

	input[class*="rounded-2xl"],
	select[class*="rounded-2xl"],
	textarea[class*="rounded-2xl"] {
		border-color: rgba(16, 24, 32, 0.12) !important;
		background: rgba(255, 255, 255, 0.72) !important;
		color: #101820;
		transition:
			border-color 0.18s ease,
			box-shadow 0.18s ease,
			background-color 0.18s ease;
	}

	input[class*="rounded-2xl"]:focus,
	select[class*="rounded-2xl"]:focus,
	textarea[class*="rounded-2xl"]:focus {
		border-color: rgba(40, 224, 143, 0.9) !important;
		background: white !important;
		box-shadow: 0 0 0 4px rgba(40, 224, 143, 0.14);
	}

	[data-order-list] {
		display: grid;
		gap: 1rem;
	}

	.order-card {
		border-radius: 1.6rem !important;
		transition:
			transform 0.2s ease,
			box-shadow 0.2s ease,
			border-color 0.2s ease;
	}

	[data-tab-panel] h2,
	[data-tab-panel] h3,
	[data-tab-panel] p[class*="font-semibold"],
	[data-tab-panel] p[class*="font-medium"] {
		letter-spacing: -0.01em;
	}

	[data-tab-panel] svg {
		transition: transform 0.24s ease;
	}

	.stat-card:hover svg,
	.metric-card:hover svg,
	.order-card:hover svg {
		transform: scale(1.08);
	}

	@media (max-width: 760px) {
		.admin-tabs {
			top: 3.8rem;
			border-radius: 1.4rem;
		}

		.admin-tabs-link {
			margin-left: 0;
		}
	}
</style>
